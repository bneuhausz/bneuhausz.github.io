const s=`---
title: Customizing Angular Material 20
slug: angular-material-20-theming
description: Some notes about customization and theming in Angular Material 20
date: 2025-08-16
coverImage: /images/angular_wordmark_gradient_header.avif
coverImageMedium: /images/angular_wordmark_gradient_header_medium.avif
coverImageSmall: /images/angular_wordmark_gradient_header_small.avif
coverImageDescription: angular gradient image
metaImage: https://bneuhausz.dev/images/bneuhausz_dev_twitter.png
metaImageDescription: bneuhausz.dev site logo
thumbnail: /images/angular_gradient_thumbnail.avif
thumbnailDescription: angular logo
icon: /images/angular_gradient_icon.avif
iconDescription: angular logo
tags: [JavaScript, Angular, Angular Material, Theming]
shadowColor: angular
draft: false
---

<h1 id="customizing-angular-material-20">Customizing Angular Material 20</h1>
<p>Again, this is just a quick write up for myself, but if for some reason you get here, you&#39;re very welcome to use it. You&#39;re not likely to find anything you wouldn&#39;t find in the <a href="https://v20.material.angular.dev/">official documentation</a> though. The purpose of this is mostly just to have some notes that are specifically relevant to me in one place. Anyway, let&#39;s get into it.</p><h2 id="some-general-thoughts">Some general thoughts</h2>
<p>So Angular Material, at least in recent versions, heavily builds on CSS variables and Sass mixins. This greatly reduces the pain customizing older versions caused for many of us.</p><p>The Angular material theme defines system variables, that you can easily override, both on the <code>:root</code> scope or scoped to anything you want to.</p><p>You can customize components through tokens they expose. These tokens often rely on a system variable, so changing those might be enough to get the effect you want, but through the supported mixins you can set these tokens to whatever you want to.</p><h3 id="applying-a-theme">Applying a theme</h3>
<p>Since we are using Angular Material, the assumption will obviously be that we are talking about an Angular application, so unless you want to handle things is a more finegrained way, at least for demonstration purposes, everything can be put into <code>styles.scss</code>.</p><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#F97583">@use</span><span style="color:#9ECBFF"> '@angular/material'</span><span style="color:#F97583"> as</span><span style="color:#FFAB70"> mat</span><span style="color:#E1E4E8">;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#B392F0">:root</span><span style="color:#E1E4E8"> {</span></span>
<span class="line"><span style="color:#F97583">  @include</span><span style="color:#FFAB70"> mat</span><span style="color:#E1E4E8">.</span><span style="color:#B392F0">theme</span><span style="color:#E1E4E8">((</span></span>
<span class="line"><span style="color:#79B8FF">    color</span><span style="color:#E1E4E8">: (</span></span>
<span class="line"><span style="color:#E1E4E8">      primary: </span><span style="color:#FFAB70">mat</span><span style="color:#E1E4E8">.</span><span style="color:#FFAB70">$azure-palette</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#E1E4E8">      tertiary: </span><span style="color:#FFAB70">mat</span><span style="color:#E1E4E8">.</span><span style="color:#FFAB70">$blue-palette</span></span>
<span class="line"><span style="color:#E1E4E8">    ),</span></span>
<span class="line"><span style="color:#E1E4E8">    typography: Roboto,</span></span>
<span class="line"><span style="color:#E1E4E8">    density: </span><span style="color:#79B8FF">-5</span></span>
<span class="line"><span style="color:#E1E4E8">  ));</span></span>
<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>
<p>This is a fairly basic example, but covers most of what you&#39;ll likely need.</p><p>This example uses the prebuilt azure and blue palettes provided by Angular Material as its primary and tertiary colors, respectively. The tertiary pallet is special, as it is rarely used by the built in components and it is meant to serve as a distinct, accent color. If the tertiary palette is not provided, Angular Material will use the primary palette in place of the tertiary palette too.</p><p>Typography is also set to Roboto here, meaning, your components will use that as their font. If you want to change the font to something else globally, you can do that here.</p><p>Density controls the spacing withing the components. Acceptable values are 0 to -5, 0 being the default. The lower the value is, the less padding there is within components. Not every component is affected by this, but this is a very useful setting to be aware of if you need to show a lot of data on a page, as it greatly reduces the space needed by your table rows, toolbars, even your buttons in some cases.</p><p>The minimum setup to make this work with a single palette and default values is the following:</p><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#F97583">@use</span><span style="color:#9ECBFF"> '@angular/material'</span><span style="color:#F97583"> as</span><span style="color:#FFAB70"> mat</span><span style="color:#E1E4E8">;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#B392F0">:root</span><span style="color:#E1E4E8"> {</span></span>
<span class="line"><span style="color:#F97583">  @include</span><span style="color:#FFAB70"> mat</span><span style="color:#E1E4E8">.</span><span style="color:#B392F0">theme</span><span style="color:#E1E4E8">((</span></span>
<span class="line"><span style="color:#79B8FF">    color</span><span style="color:#E1E4E8">: </span><span style="color:#FFAB70">mat</span><span style="color:#E1E4E8">.</span><span style="color:#FFAB70">$azure-palette</span></span>
<span class="line"><span style="color:#E1E4E8">  ));</span></span>
<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>
<h2 id="system-variables">System variables</h2>
<p>These variables cover colors, typography and elevation. The official documentation with the complete list of available variables can be found <a href="https://material.angular.dev/guide/system-variables">here.</a></p><h3 id="colors">Colors</h3>
<p>Angular Material uses many color variables, so I&#39;ll not cover them all, only the most important ones and some basic concepts about their usage.</p><p>Probably the most important variables, or more accurately, pair of variables, are <code>--mat-sys-primary</code> and <code>--mat-sys-on-primary</code>. The dominant color of your app, other than possibly the background color itself will likely be <code>--mat-sys-primary</code>. If you use a prebuilt theme or you use the Angular CLI and create your own theme (more on this later) through its <code>@angular/material:theme-color</code> schematic, <code>--mat-sys-primary</code> and some other variables, like <code>--mat-sys-error</code> for example, will be used to calculate their pair color for optimal accessibility and legibility.</p><p>Meaning, whenever you use <code>--mat-sys-primary</code> as the background color of a component, its pair <code>--mat-sys-on-primary</code> should be used as the color of text, icons, or anything else really, that should be visible on it. The concept is exactly the same when it comes to <code>--mat-sys-error</code> and <code>--mat-sys-on-error</code>, <code>--mat-sys-secondary</code> and <code>--mat-sys-secondary</code> and a few more variables you can look up in the documentation.</p><p>There are some special color variables when it comes to dark mode. We will talk about implementing dark mode later, but for now, let&#39;s just note that the color variables that contain &quot;fixed&quot; in their name are special colors that are not changed based on the application using light or dark mode. Some examples of these are <code>--mat-sys-primary-fixed</code> and <code>mat-sys-primary-fixed-dim</code>.</p><h3 id="typography">Typography</h3>
<p>These variables, not surprisingly, are meant to control how your fonts behave.</p><p>Angular Material provides these variables in 5 categories: </p><ul>
<li>body</li>
<li>display</li>
<li>headline</li>
<li>label</li>
<li>title</li>
</ul>
<p>Each category has 3 sizes defined:</p><ul>
<li>small</li>
<li>medium</li>
<li>large</li>
</ul>
<p>An example for a variable like this is <code>--mat-sys-body-medium</code>. These variables can be applied to the <code>font</code> css property like this:</p><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#B392F0">.example-class</span><span style="color:#E1E4E8"> {</span></span>
<span class="line"><span style="color:#79B8FF">  font</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">var</span><span style="color:#E1E4E8">(</span><span style="color:#FFAB70">--mat-sys-body-medium</span><span style="color:#E1E4E8">)</span></span>
<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>
<p>The default definition of a variable like this might look like <code>--mat-sys-body-medium: 400 0.875rem / 1.25rem Roboto, sans-serif;</code>. Angular Material lets you access parts of this separately by appending certain keywords.</p><p>Assuming <code>--mat-sys-body-medium</code> has the value from the example above, it would be equivalent to the following:</p><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#B392F0">.example-class</span><span style="color:#E1E4E8"> {</span></span>
<span class="line"><span style="color:#FFAB70">  --mat-sys-body-medium-font</span><span style="color:#E1E4E8">: Roboto, </span><span style="color:#79B8FF">sans-serif</span><span style="color:#E1E4E8">;</span></span>
<span class="line"><span style="color:#FFAB70">  --mat-sys-body-medium-line-height</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">1.25</span><span style="color:#F97583">rem</span><span style="color:#E1E4E8">;</span></span>
<span class="line"><span style="color:#FFAB70">  --mat-sys-body-medium-size</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">0.875</span><span style="color:#F97583">rem</span><span style="color:#E1E4E8">;</span></span>
<span class="line"><span style="color:#FFAB70">  --mat-sys-body-medium-tracking</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">0.016</span><span style="color:#F97583">rem</span><span style="color:#E1E4E8">;</span></span>
<span class="line"><span style="color:#FFAB70">  --mat-sys-body-medium-weight</span><span style="color:#E1E4E8">: </span><span style="color:#79B8FF">400</span><span style="color:#E1E4E8">;</span></span>
<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>
<h3 id="elevation">Elevation</h3>
<p>These variables are basically serve to control box-shadow, effectively giving a bit of a 3D look to certain components, making them appear a bit closer to you. Angular Material provides 6 of these variables, from <code>mat-sys-level0</code> to <code>mat-sys-level5</code>, level0 meaning no box shadow at all and level5 having the most prominent visuals.</p><p>These variables can be applied to the <code>box-shadow</code> css property like <code>box-shadow: var(mat-sys-level0)</code></p><h3 id="overriding-system-variables">Overriding system variables</h3>
<p>System variabled can be overridden through the <code>mat.theme-overrides</code> mixin. Let&#39;s say you want to override the primary color variable for whatever reason. This is as simple as this:</p><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#F97583">@use</span><span style="color:#9ECBFF"> '@angular/material'</span><span style="color:#F97583"> as</span><span style="color:#FFAB70"> mat</span><span style="color:#E1E4E8">;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#F97583">@include</span><span style="color:#FFAB70"> mat</span><span style="color:#E1E4E8">.</span><span style="color:#B392F0">theme-overrides</span><span style="color:#E1E4E8">((</span></span>
<span class="line"><span style="color:#E1E4E8">  primary: </span><span style="color:#79B8FF">#ffffff</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#E1E4E8">));</span></span></code></pre>
<p>You can do this on any scope, but we will talk about scoping later. Notice that, inside the mixin, you have to omit the <code>mat-sys</code> prefix from the name of the variables you want to override.</p><h2 id="dark-mode">Dark mode</h2>
<p>Newer versions of Angular Material use the <code>light-dark()</code> css function to calculate color by default. The browser support is growing, <a href="https://caniuse.com/mdn-css_types_color_light-dark">it is 83.26% at the time of writing this,</a> so you should be fine when it comes to creating new applications, but keep that in mind. Setting up dark mode support in older versions is a bit more complicated, so unless you have very good reasons to opt for the legacy way of handling it, you probably shouldn&#39;t.</p><p>Either way, in newer versions, Angular Material themes contain automatically calculated dark mode colors for your default colors. What that means for us is that, for example, the <code>--mat-sys-primary</code> system variable is defined like <code>--mat-sys-primary: light-dark(hex-code-of-primary-color, hex-code-of-automatically-calculated-dark-mode-color)</code>.</p><p>You can control the state of using light or dark mode by setting the <code>color-scheme</code> css attribute on the body element for example.</p><p>Some of the possible values are:</p><ul>
<li><code>light</code> - forcing light mode</li>
<li><code>dark</code> - forcing dark mode</li>
<li><code>light dark</code> - letting the browser decide based on the users settings.</li>
</ul>
<p>A bit more finegrained control can be achieved, but that&#39;s not really relevant for us here. More can be found about that <a href="https://developer.mozilla.org/en-US/docs/Web/CSS/color-scheme">here.</a></p><h2 id="styling-components">Styling components</h2>
<p>We will use the <code>mat-toolbar</code> and <code>mat-button</code> components as examples, because noth are staples in any kind of application. The toolbar seems like the more complex component, but as you will see, surprisingly, the button offers a lot more customizability.</p><p>By introducing system variables, override mixins and design tokens, Angular Material became a lot less opinionated, or at the very least, a lot more customizable. <a href="https://material.angular.dev/components/toolbar/styling">A new tab labeled styling</a> showed up in the documentation of the components. This tab provides a list of mixins and design tokens avaialble for the given component. You can also find out here if a token is based on a system token.</p><p>Tokens can be provided in 4 categories:</p><ul>
<li>Color</li>
<li>Typography</li>
<li>Density</li>
<li>Base</li>
</ul>
<p>The first 3 are fairly self explanatory. The base category is, well, for everything else. For example, you could control the shape of a filled button with the <code>filled-container-shape</code> token. More on that later.</p><p>A component might only offer a single mixin with a few tokens, as is the case with <code>mat-toolbar</code>, or it might offer multiple mixins with a ton of tokens, like the <code>mat-button</code> component.</p><p>Anyway, let&#39;s jump into it through an example. The <code>mat-toolbar</code> can be overridden by a single mixing, <code>mat.toolbar-overrides</code>. Let&#39;s say you want to override every single toolbar component in your application. In that case, all you have to do is to add this to your <code>styles.scss</code> file:</p><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#F97583">@use</span><span style="color:#9ECBFF"> '@angular/material'</span><span style="color:#F97583"> as</span><span style="color:#FFAB70"> mat</span><span style="color:#E1E4E8">;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#B392F0">:root</span><span style="color:#E1E4E8"> {</span></span>
<span class="line"><span style="color:#F97583">  @include</span><span style="color:#FFAB70"> mat</span><span style="color:#E1E4E8">.</span><span style="color:#B392F0">toolbar-overrides</span><span style="color:#E1E4E8">((</span></span>
<span class="line"><span style="color:#E1E4E8">    container</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">background</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">color: </span><span style="color:#79B8FF">var</span><span style="color:#E1E4E8">(</span><span style="color:#FFAB70">--mat-sys-primary</span><span style="color:#E1E4E8">),</span></span>
<span class="line"><span style="color:#E1E4E8">    container</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">text</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">color: </span><span style="color:#79B8FF">var</span><span style="color:#E1E4E8">(</span><span style="color:#FFAB70">--mat-sys-on-primary</span><span style="color:#E1E4E8">),</span></span>
<span class="line"><span style="color:#E1E4E8">    title</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">text</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">size: </span><span style="color:#79B8FF">var</span><span style="color:#E1E4E8">(</span><span style="color:#FFAB70">--mat-sys-label-large</span><span style="color:#E1E4E8">),</span></span>
<span class="line"><span style="color:#E1E4E8">  ));</span></span>
<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>
<p>This is all fairly self explanatory, but basically, if you include this snippet in your global css, every single <code>mat-toolbar</code> in your application will have your <code>--mat-sys-primary</code>color as its background color, text on it will be rendered in your <code>--mat-sys-on-primary</code> color and the font on it will be set to <code>--mat-sys-label-large</code>. These colors make your toolbar stand out from the background and sets the text size to a smaller value. You might not want this by default, but with <code>density: -5</code> being set on the theme, this makes the text&#39;s size more in line with the toolbar itself.</p><p>Now let&#39;s talk a bit about <code>mat-button</code>. If you think about it, a button can take up a lot of forms, so it&#39;s not really that surprising that it is the more complex component of the two from a design perspective.</p><p>Angular Material separates buttons in three distinct categories (fab, icon, everything else) to begin with and offers 3 mixins because of that:</p><ul>
<li><code>fab-overrides</code></li>
<li><code>icon-button-overrides</code></li>
<li><code>button-overrides</code></li>
</ul>
<p>Within these mixins, dozens of design tokens are available. At the time of writing this, <code>fab-overrides</code> offers 31 tokens only in the <code>Color</code> category and we didn&#39;t even mention the other mixins. The <code>mat-toolbar</code> didn&#39;t offer any tokens that were based on system tokens, but <code>mat-button</code> is different in this aspect too. For example, the <code>disabled-state-container-color</code> token within the <code>fab-overrides</code> mixin is based on the <code>--mat-sys-on-surface</code> token, meaning, if you change the value of that system variable, you can control how the fab button looks in disabled state.</p><p>That said, we&#39;ve talked about color tokens in the toolbar example, so let&#39;s look at soemthing from the <code>Base</code> category here, since no token is available from this category on the toolbar. Let&#39;s say you want a button that looks like a primary button from the Material 2 times. You would start with <code><button matButton="filled">Basic</button></code> as the filled button already has the primary coloring by default. To achieve the classic, less rounded, Material 2 look, you just have to add:</p><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#F97583">@use</span><span style="color:#9ECBFF"> '@angular/material'</span><span style="color:#F97583"> as</span><span style="color:#FFAB70"> mat</span><span style="color:#E1E4E8">;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#B392F0">:root</span><span style="color:#E1E4E8"> {</span></span>
<span class="line"><span style="color:#F97583">  @include</span><span style="color:#FFAB70"> mat</span><span style="color:#E1E4E8">.</span><span style="color:#B392F0">button-overrides</span><span style="color:#E1E4E8">((</span></span>
<span class="line"><span style="color:#E1E4E8">    filled</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">container</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">shape: </span><span style="color:#79B8FF">4</span><span style="color:#F97583">px</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#E1E4E8">  ));</span></span>
<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>
<p>Just like that, all of your filled buttons have the distinct Material 2 look.</p><h2 id="scoping">Scoping</h2>
<p>Now let&#39;s talk about scoping. Everything we&#39;ve done so far was on the <code>:root</code> scope, which is great, if you want to have a default design for each occurance of a certain type of component in your app. However, even withing these basic examples we&#39;ve covered, there might be need for scoped definitions. Let&#39;s say you want to add a <code>matIconButton</code> to your toolbar. Well, by default, the icon on the buttons has a color that is distinct on the surface colors. However, that might not be very visible on your primary color. In the example above, the toolbar is using the prebuilt azure palette&#39;s primary color, which is a dark blue, while the icons are pretty dark grey by default. That is obviously not too visible. However, if you globally change the icon color to <code>--mat-sys-on-primary</code>, which is white in this case, it won&#39;t be visible on surfaces that are not set to have the primary background color, like tables for example.</p><p>How do you fix that? Easily!</p><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#85E89D">mat-toolbar</span><span style="color:#E1E4E8"> {</span></span>
<span class="line"><span style="color:#F97583">  @include</span><span style="color:#FFAB70"> mat</span><span style="color:#E1E4E8">.</span><span style="color:#B392F0">toolbar-overrides</span><span style="color:#E1E4E8">((</span></span>
<span class="line"><span style="color:#E1E4E8">    container</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">background</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">color: </span><span style="color:#79B8FF">var</span><span style="color:#E1E4E8">(</span><span style="color:#FFAB70">--mat-sys-primary</span><span style="color:#E1E4E8">),</span></span>
<span class="line"><span style="color:#E1E4E8">    container</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">text</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">color: </span><span style="color:#79B8FF">var</span><span style="color:#E1E4E8">(</span><span style="color:#FFAB70">--mat-sys-on-primary</span><span style="color:#E1E4E8">),</span></span>
<span class="line"><span style="color:#E1E4E8">    title</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">text</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">size: </span><span style="color:#79B8FF">var</span><span style="color:#E1E4E8">(</span><span style="color:#FFAB70">--mat-sys-label-large</span><span style="color:#E1E4E8">),</span></span>
<span class="line"><span style="color:#E1E4E8">  ));</span></span>
<span class="line"></span>
<span class="line"><span style="color:#F97583">  @include</span><span style="color:#FFAB70"> mat</span><span style="color:#E1E4E8">.</span><span style="color:#B392F0">icon-button-overrides</span><span style="color:#E1E4E8">((</span></span>
<span class="line"><span style="color:#E1E4E8">    icon</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">color: </span><span style="color:#79B8FF">var</span><span style="color:#E1E4E8">(</span><span style="color:#FFAB70">--mat-sys-on-primary</span><span style="color:#E1E4E8">),</span></span>
<span class="line"><span style="color:#E1E4E8">  ));</span></span>
<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>
<p>In this example, assuming this is in your <code>styles.scss</code>, we&#39;ve set up every single occurance of <code>mat-toolbar</code> in your application to have the same config we previously used and we&#39;ve also set the <code>icon-color</code> token of every <code>matIconButton</code> that is inside a <code>mat-toolbar</code>. However, we did not alter the design of the <code>matIconButton</code> occurances that are outside of a <code>mat-toolbar</code>.</p><p>In a similar vein, you can set up entirely different themes in different scopes.</p><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#F97583">@use</span><span style="color:#9ECBFF"> '@angular/material'</span><span style="color:#F97583"> as</span><span style="color:#FFAB70"> mat</span><span style="color:#E1E4E8">;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#B392F0">:root</span><span style="color:#E1E4E8"> {</span></span>
<span class="line"><span style="color:#F97583">  @include</span><span style="color:#FFAB70"> mat</span><span style="color:#E1E4E8">.</span><span style="color:#B392F0">theme</span><span style="color:#E1E4E8">((</span></span>
<span class="line"><span style="color:#79B8FF">    color</span><span style="color:#E1E4E8">: (</span></span>
<span class="line"><span style="color:#E1E4E8">      primary: </span><span style="color:#FFAB70">mat</span><span style="color:#E1E4E8">.</span><span style="color:#FFAB70">$azure-palette</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#E1E4E8">      tertiary: </span><span style="color:#FFAB70">mat</span><span style="color:#E1E4E8">.</span><span style="color:#FFAB70">$blue-palette</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#E1E4E8">    ),</span></span>
<span class="line"><span style="color:#E1E4E8">    typography: Roboto,</span></span>
<span class="line"><span style="color:#E1E4E8">    density: </span><span style="color:#79B8FF">-5</span></span>
<span class="line"><span style="color:#E1E4E8">  ));</span></span>
<span class="line"><span style="color:#E1E4E8">}</span></span>
<span class="line"></span>
<span class="line"><span style="color:#85E89D">mat-toolbar</span><span style="color:#E1E4E8"> {</span></span>
<span class="line"><span style="color:#F97583">  @include</span><span style="color:#FFAB70"> mat</span><span style="color:#E1E4E8">.</span><span style="color:#B392F0">theme</span><span style="color:#E1E4E8">((</span></span>
<span class="line"><span style="color:#79B8FF">    color</span><span style="color:#E1E4E8">: </span><span style="color:#FFAB70">mat</span><span style="color:#E1E4E8">.</span><span style="color:#FFAB70">$orange-palette</span></span>
<span class="line"><span style="color:#E1E4E8">  ));</span></span>
<span class="line"></span>
<span class="line"><span style="color:#F97583">  @include</span><span style="color:#FFAB70"> mat</span><span style="color:#E1E4E8">.</span><span style="color:#B392F0">toolbar-overrides</span><span style="color:#E1E4E8">((</span></span>
<span class="line"><span style="color:#E1E4E8">    container</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">background</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">color: </span><span style="color:#79B8FF">var</span><span style="color:#E1E4E8">(</span><span style="color:#FFAB70">--mat-sys-primary</span><span style="color:#E1E4E8">),</span></span>
<span class="line"><span style="color:#E1E4E8">    container</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">text</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">color: </span><span style="color:#79B8FF">var</span><span style="color:#E1E4E8">(</span><span style="color:#FFAB70">--mat-sys-on-primary</span><span style="color:#E1E4E8">),</span></span>
<span class="line"><span style="color:#E1E4E8">    title</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">text</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">size: </span><span style="color:#79B8FF">var</span><span style="color:#E1E4E8">(</span><span style="color:#FFAB70">--mat-sys-label-large</span><span style="color:#E1E4E8">),</span></span>
<span class="line"><span style="color:#E1E4E8">  ));</span></span>
<span class="line"></span>
<span class="line"><span style="color:#F97583">  @include</span><span style="color:#FFAB70"> mat</span><span style="color:#E1E4E8">.</span><span style="color:#B392F0">icon-button-overrides</span><span style="color:#E1E4E8">((</span></span>
<span class="line"><span style="color:#E1E4E8">    icon</span><span style="color:#79B8FF">-</span><span style="color:#E1E4E8">color: </span><span style="color:#79B8FF">var</span><span style="color:#E1E4E8">(</span><span style="color:#FFAB70">--mat-sys-on-primary</span><span style="color:#E1E4E8">),</span></span>
<span class="line"><span style="color:#E1E4E8">  ));</span></span>
<span class="line"><span style="color:#E1E4E8">}</span></span></code></pre>
<p>Notice the usage of the <code>mat.theme</code> mixin inside the <code>mat-toolbar</code> tag. With this setup, the entire application will use the azure palette, except every single <code>mat-toolbar</code> in your application, which will default to the orange palette. If you need even more scoped designe that that, you can just use the <code>mat.theme</code> mixin inside your Angular component and the usual rules regarding Angular styles and scoping will apply.</p><h2 id="creating-a-custom-theme">Creating a custom theme</h2>
<p>The Angular CLI offers a schematic for generating your custom theme. All you have to do is add Angular Material:</p><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#B392F0">ng</span><span style="color:#9ECBFF"> add</span><span style="color:#9ECBFF"> @angular/material</span></span></code></pre>
<p>Then you can just run the following command:</p><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#B392F0">ng</span><span style="color:#9ECBFF"> generate</span><span style="color:#9ECBFF"> @angular/material:theme-color</span></span></code></pre>
<p>The CLI will guide you through the process, but basically the minimum you need to provide is the hex code of the color you want to use as your primary color.
You can read about it more <a href="https://github.com/angular/components/blob/main/src/material/schematics/ng-generate/theme-color/README.md">here.</a></p>`;export{s as default};
