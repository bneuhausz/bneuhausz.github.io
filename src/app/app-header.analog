<script lang="ts">
import { RouterLink } from '@angular/router' with { analog: 'imports' };
import { NgIcon } from '@ng-icons/core' with { analog: 'imports' };
</script>

<template>
  <nav class="navbar bg-neutral text-neutral-content gap-2">
    <a class="btn btn-square btn-ghost hover:text-neutral" routerLink="/">
      <ng-icon name="heroHomeSolid"></ng-icon>
    </a>
    <a class="btn btn-square btn-ghost hover:text-neutral px-8" routerLink="/blog">
      Blog
    </a>
  </nav>
</template>