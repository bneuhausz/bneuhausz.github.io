<script lang="ts">
  import { RouterOutlet } from '@angular/router' with { analog: 'imports' };
  import AppHeader from './app-header.analog' with { analog: 'imports' };
  import AppFooter from './app-footer.analog' with { analog: 'imports' };
</script>

<template class="flex flex-col h-screen">
  <AppHeader />

  <main class="flex-1 p-4">
    <router-outlet />
  </main>

  <AppFooter />
</template>